name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────
  # Validate the tag matches pyproject.toml version
  # ──────────────────────────────────────────────
  validate:
    name: "Validate Release"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.version.outputs.version }}
      sha: ${{ steps.version.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: core/pyproject.toml
      - id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          PYPROJECT_VERSION=$(python -c "
          import re
          with open('core/pyproject.toml') as f:
              m = re.search(r'version\s*=\s*\"([^\"]+)\"', f.read())
              print(m.group(1) if m else '')
          ")
          echo "Tag version: $TAG"
          echo "pyproject.toml version: $PYPROJECT_VERSION"
          if [ "$TAG" != "$PYPROJECT_VERSION" ]; then
            echo "ERROR: Tag v$TAG does not match pyproject.toml version $PYPROJECT_VERSION"
            exit 1
          fi
          echo "version=$TAG" >> "$GITHUB_OUTPUT"
          echo "sha=$GITHUB_SHA" >> "$GITHUB_OUTPUT"

  # ──────────────────────────────────────────────
  # Run full test suite before release
  # ──────────────────────────────────────────────
  test:
    name: "Release Tests"
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: core
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: core/pyproject.toml
      - run: pip install -e ".[dev]"
      - run: python -m pytest tests/ -x -q

  # ──────────────────────────────────────────────
  # Docker build smoke test (required before push)
  # ──────────────────────────────────────────────
  docker-build:
    name: "Docker Build"
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build core image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.core
          load: true
          tags: scholardevclaw-core:validate
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build agent image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.agent
          load: true
          tags: scholardevclaw-agent:validate
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ──────────────────────────────────────────────
  # Build and publish to PyPI
  # ──────────────────────────────────────────────
  publish-pypi:
    name: "Publish to PyPI"
    needs: [validate, test]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: release
    defaults:
      run:
        working-directory: core
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install build twine
      - run: python -m build
      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: twine upload dist/*

  # ──────────────────────────────────────────────
  # Create GitHub Release with changelog
  # ──────────────────────────────────────────────
  github-release:
    name: "GitHub Release"
    needs: [validate, publish-pypi]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Extract changelog for this version
        id: changelog
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          # Extract the section for this version from CHANGELOG.md
          python3 -c "
          import re, sys
          with open('CHANGELOG.md') as f:
              content = f.read()
          pattern = rf'## \[{re.escape(\"$VERSION\")}\].*?\n(.*?)(?=\n## \[|$)'
          m = re.search(pattern, content, re.DOTALL)
          if m:
              print(m.group(1).strip())
          else:
              print('Release v\$VERSION')
          " > release_notes.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_notes.md
          generate_release_notes: true

  # ──────────────────────────────────────────────
  # Build and push Docker images
  # ──────────────────────────────────────────────
  docker-publish:
    name: "Docker Publish"
    needs: [validate, docker-build]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push core
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.core
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/core:${{ needs.validate.outputs.version }}
            ghcr.io/${{ github.repository }}/core:${{ needs.validate.outputs.sha }}
            ghcr.io/${{ github.repository }}/core:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build and push agent
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.agent
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/agent:${{ needs.validate.outputs.version }}
            ghcr.io/${{ github.repository }}/agent:${{ needs.validate.outputs.sha }}
            ghcr.io/${{ github.repository }}/agent:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
