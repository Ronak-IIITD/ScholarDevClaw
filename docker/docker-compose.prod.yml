version: '3.8'

services:
  web-ui:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
      target: production
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - scholardevclaw-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  core-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.core
      target: production
    restart: unless-stopped
    expose:
      - "8000"
    volumes:
      - repos-data:/repos:ro
      - logs-data:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FORMAT=json
      - SCHOLARDEVCLAW_API_AUTH_KEY=${SCHOLARDEVCLAW_API_AUTH_KEY:?SCHOLARDEVCLAW_API_AUTH_KEY must be set}
      - SCHOLARDEVCLAW_ALLOWED_REPO_DIRS=${SCHOLARDEVCLAW_ALLOWED_REPO_DIRS:?SCHOLARDEVCLAW_ALLOWED_REPO_DIRS must be set}
      - SCHOLARDEVCLAW_ENABLE_HSTS=${SCHOLARDEVCLAW_ENABLE_HSTS:-true}
      - SCHOLARDEVCLAW_CORS_ORIGINS=${SCHOLARDEVCLAW_CORS_ORIGINS:?SCHOLARDEVCLAW_CORS_ORIGINS must be set}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - scholardevclaw-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile.agent
      target: production
    restart: unless-stopped
    depends_on:
      core-api:
        condition: service_healthy
    environment:
      - NODE_ENV=production
      - CORE_API_URL=http://core-api:8000
      - SCHOLARDEVCLAW_API_AUTH_KEY=${SCHOLARDEVCLAW_API_AUTH_KEY:?SCHOLARDEVCLAW_API_AUTH_KEY must be set}
      - OPENCLAW_TOKEN=${OPENCLAW_TOKEN}
      - OPENCLAW_API_URL=${OPENCLAW_API_URL}
      - CONVEX_URL=${CONVEX_URL}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CORE_BRIDGE_MODE=${CORE_BRIDGE_MODE:-http}
      - SCHOLARDEVCLAW_API_PROVIDER=${SCHOLARDEVCLAW_API_PROVIDER:-anthropic}
      - SCHOLARDEVCLAW_API_MODEL=${SCHOLARDEVCLAW_API_MODEL:-claude-sonnet-4-20250514}
    volumes:
      - agent-data:/app/data
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
    networks:
      - scholardevclaw-network
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - web-ui
      - core-api
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - scholardevclaw-network

  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    expose:
      - "9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    networks:
      - scholardevclaw-network

  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    expose:
      - "3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:?GRAFANA_ADMIN_USER must be set}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD must be set}
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus
    networks:
      - scholardevclaw-network

networks:
  scholardevclaw-network:
    driver: bridge

volumes:
  repos-data:
  logs-data:
  agent-data:
  prometheus-data:
  grafana-data:
