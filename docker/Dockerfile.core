FROM python:3.11-slim

WORKDIR /app

COPY core/pyproject.toml ./
RUN pip install --no-cache-dir -e .

COPY core/src/ ./src/

EXPOSE 8000

CMD ["uvicorn", "scholardevclaw.api.server:app", "--host", "0.0.0.0", "--port", "8000"]
